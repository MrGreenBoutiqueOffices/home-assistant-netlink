[project]
name = "netlink"
version = "0.0.0"
description = "NetLink integration for Home Assistant"
authors = [{ name = "Klaas Schoute", email = "k.schoute@mrgreenoffices.nl"}]
maintainers = [{name = "Klaas Schoute", email = "k.schoute@mrgreenoffices.nl"}]
license = "LGPL-3.0-only"
readme = "README.md"
keywords = [
  "custom integration",
  "home assistant",
  "home-assistant",
  "homeassistant",
  "integration",
  "netlink",
]
classifiers = [
  "Development Status :: 5 - Production/Stable",
  "Framework :: AsyncIO",
  "Intended Audience :: Developers",
  "Natural Language :: English",
  "Programming Language :: Python :: 3.14",
  "Programming Language :: Python :: 3",
]
requires-python = ">=3.14.2"
dependencies = [
    "homeassistant>=2026.5.0",
]

[dependency-groups]
dev = [
    "covdefaults==2.3.0",
    "coverage==7.15.2",
    "pre-commit-hooks==6.0.0",
    "prek==0.4.14",
    "pynetlink==1.5.0",
    "pytest-asyncio==1.4.0",
    "pytest-cov==7.1.0",
    "pytest-homeassistant-custom-component==0.13.356",
    "ruff==0.15.22",
]

[tool.coverage.run]
plugins = ["covdefaults"]
source = ["custom_components"]

[tool.coverage.report]
fail_under = 95
show_missing = true

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["custom_components/netlink"]

[tool.pytest.ini_options]
addopts = "--cov"
asyncio_mode = "auto"

[tool.ruff]
src = ["custom_components/netlink"]

[tool.ruff.lint.flake8-import-conventions.extend-aliases]
"homeassistant.helpers.area_registry" = "ar"
"homeassistant.helpers.config_validation" = "cv"
"homeassistant.helpers.device_registry" = "dr"
"homeassistant.helpers.entity_registry" = "er"
"homeassistant.helpers.issue_registry" = "ir"
voluptuous = "vol"

[tool.ruff.lint.isort]
force-sort-within-sections = true
known-first-party = [
    "homeassistant",
]
combine-as-imports = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
